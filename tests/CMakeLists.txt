# Enable these lines once key_value_db_tests.cpp contains a test runner.
# The runner should return zero on success and nonzero on failure.
# add_executable(keyValueDB_tests key_value_db_tests.cpp)
# target_link_libraries(keyValueDB_tests PRIVATE keyValueDB_core)
# target_compile_features(keyValueDB_tests PRIVATE cxx_std_17)
# set_target_properties(keyValueDB_tests PROPERTIES CXX_EXTENSIONS OFF)
# add_test(NAME keyValueDB_tests COMMAND keyValueDB_tests)
